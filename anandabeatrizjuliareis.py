# Exercício 8: O Caçador de Vogais
# Dada a lista com algumas letras do alfabeto: letras = ['a', 'b', 'c', 'e', 'f', 'i', 'j', 'o', 'x', 'u'].
# Usa um ciclo for para verificar cada letra. Se for uma vogal (a, e, i, o, u), imprime a letra no ecrã.
# (Dica: podes usar a verificação if letra in ['a', 'e', 'i', 'o', 'u']:).

letras = ['a', 'b', 'c', 'e', 'f', 'i', 'j', 'o', 'x', 'u']

for letra in letras:
    if letra in ['a', 'e', 'i', 'o', 'u']:
        print(letras)
